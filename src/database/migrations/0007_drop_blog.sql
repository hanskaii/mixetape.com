-- The blog from the kit template is gone: mixetape is a publisher only.
-- Both tables were empty when they were dropped (checked 2026-09-26).
DROP TABLE IF EXISTS `bookmarks`;
--> statement-breakpoint
DROP TABLE IF EXISTS `posts`;
